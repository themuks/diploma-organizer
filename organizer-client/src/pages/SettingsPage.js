import React from "react";
import Modal from "../components/Modal";

const SettingsPage = () => {
    return (
        <Modal>
            Settings
        </Modal>
    );
};

export default SettingsPage;